# Dependencies
node_modules/

# Build output
dist/
dist-*/

# Astro internal cache
.astro/

# Lighthouse CI local report output
.lighthouseci/

# Environment variables (never commit live secrets)
.env

# Keep .env.example committed (it has no real secrets)

# Superpowers workspace (local only)
.superpowers/

# .htpasswd — NEVER commit the real password file (commit only .htpasswd.example)
.htpasswd
public/.htpasswd
